import { useState } from 'react';import { LockKeyhole,Mail,Phone,Trophy } from 'lucide-react';import { Link,useLocation,useNavigate } from 'react-router-dom';import { useAuth } from '../../hooks/useAuth';
type Mode='email'|'phone';export function LoginPage(){const[mode,setMode]=useState<Mode>('email');const[identifier,setIdentifier]=useState('');const[password,setPassword]=useState('');const auth=useAuth();const navigate=useNavigate();const location=useLocation();async function submit(e:React.FormEvent){e.preventDefault();try{await auth.login.mutateAsync({password,...(mode==='email'?{email:identifier}:{phone:identifier})});navigate((location.state as {from?:string}|null)?.from??'/',{replace:true})}catch{}}
return <main className="min-h-dvh bg-white px-5 pb-[max(2rem,env(safe-area-inset-bottom))] pt-[max(2rem,env(safe-area-inset-top))] text-black"><div className="mx-auto max-w-md"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-[#073B8C] text-white shadow-md"><Trophy/></div><h1 className="mt-7 text-3xl font-black tracking-tight">Entrar no Chavea</h1><p className="mt-2 text-sm font-medium text-slate-500">Sua arena de campeonatos começa aqui.</p><div className="mt-7 grid grid-cols-2 rounded-2xl bg-slate-100 p-1"><button type="button" onClick={()=>{setMode('email');setIdentifier('')}} className={`rounded-xl py-3 text-sm font-bold ${mode==='email'?'bg-white text-[#073B8C] shadow-sm':'text-slate-500'}`}>E-mail</button><button type="button" onClick={()=>{setMode('phone');setIdentifier('')}} className={`rounded-xl py-3 text-sm font-bold ${mode==='phone'?'bg-white text-[#073B8C] shadow-sm':'text-slate-500'}`}>Telefone</button></div><form onSubmit={submit} className="mt-5 space-y-4"><label className="block text-sm font-bold">{mode==='email'?'E-mail':'Telefone'}<div className="mt-2 flex min-h-14 items-center gap-3 rounded-2xl border border-slate-200 px-4 focus-within:border-[#073B8C]">{mode==='email'?<Mail className="h-5 w-5 text-slate-400"/>:<Phone className="h-5 w-5 text-slate-400"/>}<input type={mode==='email'?'email':'tel'} required autoComplete={mode==='email'?'email':'tel'} value={identifier} onChange={e=>setIdentifier(e.target.value)} className="w-full outline-none" placeholder={mode==='email'?'voce@email.com':'+55 82 99999-9999'}/></div></label><label className="block text-sm font-bold">Senha<div className="mt-2 flex min-h-14 items-center gap-3 rounded-2xl border border-slate-200 px-4 focus-within:border-[#073B8C]"><LockKeyhole className="h-5 w-5 text-slate-400"/><input type="password" required minLength={10} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full outline-none" placeholder="Sua senha"/></div></label>{auth.login.isError&&<p className="rounded-xl bg-red-50 p-3 text-sm font-bold text-[#E31B23]">E-mail, telefone ou senha inválidos.</p>}<button disabled={auth.login.isPending} className="min-h-14 w-full rounded-2xl bg-[#073B8C] font-extrabold text-white shadow-md disabled:opacity-60">{auth.login.isPending?'Entrando…':'Entrar'}</button></form><div className="my-6 flex items-center gap-3 text-xs font-bold text-slate-400"><span className="h-px flex-1 bg-slate-200"/>OU<span className="h-px flex-1 bg-slate-200"/></div><a href={auth.googleLoginUrl} className="flex min-h-14 items-center justify-center rounded-2xl border border-slate-200 bg-white font-bold shadow-sm">Continuar com Google</a><p className="mt-7 text-center text-sm text-slate-500">Ainda não tem conta? <Link to="/register" className="font-extrabold text-[#073B8C]">Criar conta</Link></p></div></main>}
