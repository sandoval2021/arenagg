export type CompetitionSummary={id:string;name:string;format:'LEAGUE'|'KNOCKOUT'|'GROUPS_KNOCKOUT';participantCount:number;currentRound?:number;status:'REGISTRATION'|'READY'|'IN_PROGRESS'|'FINISHED';logoUrl?:string};
export type Standing={teamId:string;team:string;logoUrl?:string;points:number;played:number;wins:number;draws:number;losses:number;goalsFor:number;goalsAgainst:number;goalDifference:number};
const API_URL=import.meta.env.VITE_API_URL??'http://localhost:8787';
export class ApiError extends Error{constructor(public status:number,public code:string){super(code);this.name='ApiError'}}
export async function apiRequest<T>(path:string,init:RequestInit={}):Promise<T>{const headers=new Headers(init.headers);if(init.body&&!(init.body instanceof FormData)&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const response=await fetch(`${API_URL}${path}`,{...init,headers,credentials:'include'});if(!response.ok){const body=await response.json().catch(()=>({error:'REQUEST_FAILED'})) as {error?:string};throw new ApiError(response.status,body.error??'REQUEST_FAILED')}return response.status===204?(undefined as T):response.json<T>()}
export async function getMyCompetitions():Promise<CompetitionSummary[]>{return apiRequest('/api/competitions')}
export async function getStandings(competitionId:string):Promise<Standing[]>{return apiRequest(`/api/competitions/${encodeURIComponent(competitionId)}/standings`)}
